version: '3.1'

services:
  app:
    container_name: benchmarks-app
    image: petk/benchmarks:node
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    volumes:
      - .:/usr/src/app
    networks:
      - benchmarks

networks:
  benchmarks:
    driver: bridge
