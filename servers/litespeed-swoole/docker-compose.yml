version: '3.0'

services:
  benchmarks-litespeed-swoole:
    build:
      context: ../../
      dockerfile: ./servers/litespeed-swoole/Dockerfile
    container_name: benchmarks-litespeed-swoole
    volumes:
      - ../../apps/swoole-app:/usr/local/lsws/Example/html
    ports:
      - 7080:7080
      - 80:8088
    networks:
      - benchmarks

networks:
  benchmarks:
    driver: bridge
